/**
 * Shopify Storefront API — store credentials only.
 * Product variant IDs live in js/shop-catalog.js
 */
window.SHOPIFY_STORE = {
  enabled: false,
  storeDomain: 'your-store.myshopify.com',
  storefrontAccessToken: '',
  apiVersion: '2025-01',
};
